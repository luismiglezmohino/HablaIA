# Vite dev server proxy target (in Docker, set via docker-compose)
VITE_API_TARGET=http://localhost:8080

# Feature Flags
VITE_ENABLE_TTS=true
VITE_ENABLE_AI_PREDICTION=true

# Sentry (empty = disabled)
VITE_SENTRY_DSN=
