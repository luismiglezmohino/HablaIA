# Environment
APP_ENV=dev
APP_SECRET=change_me_in_production_please_use_32_chars
APP_DEBUG=1

# Database
DATABASE_URL="postgresql://app:!ChangeMe!@127.0.0.1:5432/app?serverVersion=16&charset=utf8"

# CORS
CORS_ALLOW_ORIGIN='^https?://(localhost|127\.0\.0\.1)(:[0-9]+)?$'

# Trusted Proxies (Docker: REMOTE_ADDR, Production: load balancer IPs)
TRUSTED_PROXIES=REMOTE_ADDR

# Rate Limiting (phrases/generate endpoint)
PHRASE_RATE_LIMIT=30
PHRASE_RATE_INTERVAL=60

# Phrase Generation (shared across providers)
# PHRASE_PROVIDER: gemini (default, free tier), openai, fake
PHRASE_PROVIDER=gemini
PHRASE_TEMPERATURE=0.7
PHRASE_MAX_TOKENS=256
PHRASE_TIMEOUT=10

# Gemini Configuration (default - free tier)
GEMINI_API_URL=https://generativelanguage.googleapis.com/v1beta/models
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash-lite

# OpenAI Configuration (alternative)
OPENAI_API_URL=https://api.openai.com/v1/chat/completions
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
