parameters:

services:
    _defaults:
        autowire: true
        autoconfigure: true

    App\:
        resource: '../src/'
        exclude:
            - '../src/Domain/Entity/'
            - '../src/Domain/ValueObject/'
            - '../src/Application/DTO/'
            - '../src/Kernel.php'

    # Controllers will be auto-registered when the directory exists
    # App\Infrastructure\Http\Controller\:
    #     resource: '../src/Infrastructure/Http/Controller/'
    #     tags: ['controller.service_arguments']
